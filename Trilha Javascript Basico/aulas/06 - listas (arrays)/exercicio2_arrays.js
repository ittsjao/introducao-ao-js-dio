/* 2) Crie um programa que seja capaz de percorrer uma lista de numeros e imprima cada numero
encontrado. */

const numeros = [7 , 12, 24];

for (let i = 0; i < numeros.length; i++) {
    const num = numeros[i]; 
    if(num % 2 === 0) {
        console.log(num)
    } else {
        
    }
}
